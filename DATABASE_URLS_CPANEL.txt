# 🔗 DATABASE_URL para cPanel - PostgreSQL

## Configurações do Banco de Dados

### Banco Principal: devanksi_barberboss
### Usuário: devanksi_barberboss_super
### Senha: jake0707

---

## ✅ DATABASE_URL Recomendada (PostgreSQL)

```
postgresql://devanksi_barberboss_super:jake0707@localhost:5432/devanksi_barberboss
```

**Para usar no cPanel:**
1. Copie a URL acima
2. Vá em: **Node.js App** → Sua aplicação → **Environment Variables**
3. Adicione/Edite: `DATABASE_URL`
4. Cole a URL completa

---

## 📋 Formato PostgreSQL

### Formato Geral
```
postgresql://USUARIO:SENHA@HOST:PORTA/BANCO
```

### Opções de Conexão Adicionais (Opcional)
```
postgresql://devanksi_barberboss_super:jake0707@localhost:5432/devanksi_barberboss?schema=public&connection_limit=10
```

---

## ⚠️ IMPORTANTE - PostgreSQL

1. **Porta padrão PostgreSQL:** `5432` (não 3306 como MySQL)
2. **Protocolo:** `postgresql://` ou `postgres://`
3. **Verifique se o PostgreSQL está instalado no cPanel:**
   - Alguns hosts podem não ter PostgreSQL disponível
   - Verifique em: **cPanel** → **PostgreSQL Databases**

4. **Após configurar a DATABASE_URL:**
   ```bash
   npx prisma generate
   npx prisma migrate deploy
   ```

5. **Reinicie a aplicação** no cPanel após configurar.

---

## 🔍 Testar Conexão via SSH

```bash
psql -U devanksi_barberboss_super -d devanksi_barberboss -h localhost
# Digite a senha: jake0707
```

---

## 📝 Variáveis de Ambiente Necessárias no cPanel

```
DATABASE_URL=postgresql://devanksi_barberboss_super:jake0707@localhost:5432/devanksi_barberboss
NEXTAUTH_SECRET=seu_secret_aqui
NEXTAUTH_URL=https://devank.site
NEXT_PUBLIC_APP_URL=https://devank.site
NODE_ENV=production
```

---

## 🔄 Diferenças MySQL vs PostgreSQL

| Aspecto | MySQL | PostgreSQL |
|---------|-------|------------|
| Protocolo | `mysql://` | `postgresql://` ou `postgres://` |
| Porta padrão | `3306` | `5432` |
| Arrays nativos | ❌ Não suporta | ✅ Suporta (String[]) |
| Case sensitivity | Não (geralmente) | Sim (diferencia maiúsculas/minúsculas) |

---

## 🚨 Troubleshooting PostgreSQL

### Erro: "database does not exist"
- Verifique se o banco PostgreSQL foi criado
- No cPanel: **PostgreSQL Databases** → Criar novo banco

### Erro: "role does not exist"
- Verifique se o usuário PostgreSQL foi criado
- No cPanel: **PostgreSQL Databases** → Criar novo usuário

### Erro: "password authentication failed"
- Verifique se a senha está correta
- Verifique se o usuário tem permissões no banco

### Erro: "connection refused"
- Verifique se o PostgreSQL está rodando
- Verifique se a porta está correta (5432)
- Verifique se o host está correto (geralmente `localhost`)
